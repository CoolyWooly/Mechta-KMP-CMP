CREATE TABLE FavoritesTable (
    id INTEGER NOT NULL PRIMARY KEY
);

add:
INSERT INTO FavoritesTable(id) VALUES (?);

remove:
DELETE FROM FavoritesTable WHERE id = ?;

clear:
DELETE FROM FavoritesTable;